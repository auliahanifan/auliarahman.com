# auliarahman.com

Personal website. Astro, static, no cookies.

```bash
pnpm install
pnpm dev      # http://localhost:4321
pnpm build    # dist/
```

Tulisan baru: tambah file Markdown di `src/content/writing/` dengan frontmatter `title`, `description`, `date`.
